plugins {
    alias(libs.plugins.local.android.app)
}

android {
    namespace = "ru.polescanner.droid"

    defaultConfig {
        applicationId = "ru.polescanner.droid"
        versionCode = 1
        versionName = "1.0"
    }
}

dependencies {
}
